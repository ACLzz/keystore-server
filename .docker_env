ADDR=0.0.0.0
PORT=8402
MODE="test"

POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_NAME="keystore"
POSTGRES_USERNAME="keykeeper"
POSTGRES_PASSWORD=CHANGE_PASSWORD
POSTGRES_USER=postgres                  # do not change if you doesn't have custom postgres admin name
PGPASSWORD=$POSTGRES_PASSWORD
ALLOW_REGISTRATION=true

TIMEZONE="Europe/Zaporozhye"
TOKEN_LIFETIME=3600              # (in seconds)

SALT="CHANGE_SALT"